proc main(argc: u64, argv: []string) {
    let a: u32 = +5;
    let b: u32 = -5;
    let c: f32 = -5.23;
    let mainer: f32 = 0.00;
    let procer: u32 = 3;
    debug_print("string");
    let f: f32 = -51.23;
    if a >= b {
        debug_print("a is greater than b");
    } else if !(a > b) {
        debug_print("a is not greater than b")
    }
    debug_print("this is a string");
}
