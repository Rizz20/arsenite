proc main(argc: u64, argv: []string) -> u64 {
    let a: u32 = 10;
    let b: u32 = 20;
    let c: u32 = a + b;
    let d: u32 = b - a;
    let e: u32 = a * b;
    let f: u32 = b / a;
    printf("Add: %d, Sub: %d, Mul: %d, Div: %d\n", c, d, e, f);
    return 0;
}
